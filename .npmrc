# Disallow installing into the root workspace by default; deps belong to packages.
ignore-workspace-root-check=false
# Keep dependency resolution strict and reproducible across the workspace.
shared-workspace-lockfile=true
