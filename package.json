{
  "name": "doska",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "engines": {
    "node": ">=22",
    "pnpm": ">=11"
  },
  "packageManager": "pnpm@11.8.0",
  "scripts": {
    "dev": "turbo run dev --filter=!desktop",
    "build": "turbo run build --filter=!desktop",
    "desktop": "pnpm --filter desktop dev",
    "lint": "turbo run lint",
    "type-check": "turbo run typecheck",
    "format": "prettier --write \"**/*.{ts,tsx,js,json,md}\"",
    "e2e": "turbo run e2e --filter=!desktop",
    "upd": "pnpm dlx npm-check-updates --workspaces --root -i"
  },
  "devDependencies": {
    "prettier": "^3.8.4",
    "turbo": "^2"
  }
}
