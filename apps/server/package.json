{
  "name": "server",
  "private": true,
  "version": "0.4.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently -k -n db,api -c magenta,cyan \"pnpm db:serve\" \"DATABASE_URL=postgres://127.0.0.1:5432/postgres AUTH_TRUSTED_ORIGINS=http://localhost:5173 tsx watch --env-file-if-exists=.env src/index.ts\"",
    "start": "node --import tsx src/index.ts",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:studio": "drizzle-kit studio",
    "db:serve": "tsx --env-file-if-exists=.env src/serve-dev.ts",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@doska/contract": "workspace:*",
    "@doska/file-storage": "workspace:*",
    "@doska/mcp": "workspace:*",
    "@doska/sync": "workspace:*",
    "@electric-sql/pglite": "^0.5.4",
    "@modelcontextprotocol/sdk": "^1.30.0",
    "@orpc/server": "^1.14.12",
    "better-auth": "^1.6.25",
    "drizzle-orm": "^0.45.2",
    "fastify": "^5.10.0",
    "pg": "^8.22.0"
  },
  "devDependencies": {
    "@doska/configs": "workspace:*",
    "@electric-sql/pglite-socket": "^0.2.7",
    "@orpc/client": "^1.14.12",
    "@orpc/contract": "^1.14.12",
    "@types/node": "^22",
    "@types/pg": "^8.20.0",
    "concurrently": "^10.0.4",
    "drizzle-kit": "^0.31.10",
    "eslint": "^10",
    "pino-pretty": "^13.1.3",
    "tsx": "^4.23.1",
    "typescript": "~6",
    "vitest": "^4.1.10"
  }
}
