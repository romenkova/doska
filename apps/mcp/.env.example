# The Doska server to sync against. Local dev runs on 3000; a self-hosted
# instance is wherever you put it (the same URL the desktop app syncs to).
DOSKA_URL=http://localhost:3000

# The server's single account — the AUTH_LOGIN / AUTH_PASSWORD from its .env.
DOSKA_LOGIN=admin
DOSKA_PASSWORD=change-me
