/// <reference types="vite/client" />
/// <reference types="vite-plugin-pwa/client" />

// Injected at build time from the git tag (see vite.config.ts `define`).
declare const __APP_VERSION__: string
