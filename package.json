{
  "name": "doska",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "engines": {
    "node": ">=22",
    "pnpm": ">=11"
  },
  "packageManager": "pnpm@11.25.0",
  "scripts": {
    "dev": "turbo run dev --filter=!desktop --filter=!mobile",
    "build": "turbo run build --filter=!desktop --filter=!mobile",
    "desktop": "pnpm --filter desktop dev",
    "mobile": "pnpm --filter mobile dev",
    "mobile-device": "pnpm --filter mobile exec expo run:ios --device",
    "mobile-install": "pnpm --filter mobile exec expo run:ios --device --configuration Release",
    "lint": "turbo run lint",
    "type-check": "turbo run typecheck",
    "test": "turbo run test --filter=!desktop",
    "format": "prettier --write \"**/*.{ts,tsx,js,json,md}\"",
    "e2e": "turbo run e2e --filter=!desktop",
    "e2e:ui": "pnpm --filter @doska/e2e e2e:ui",
    "e2e:container": "pnpm --filter @doska/e2e e2e:container",
    "test:install": "bash .github/scripts/install-test.sh",
    "test:selfhost": "bash .github/scripts/selfhost-smoke.sh",
    "test:selfhost:data": "bash .github/scripts/selfhost-data.sh",
    "test:selfhost:upgrade": "bash .github/scripts/selfhost-upgrade.sh",
    "test:selfhost:restore": "bash .github/scripts/selfhost-restore.sh",
    "selfhost:down": "docker compose -f docker-compose.selfhost.yml --profile https down --volumes --remove-orphans",
    "upd": "pnpm dlx npm-check-updates --workspaces --root -i"
  },
  "devDependencies": {
    "prettier": "^3.9.6",
    "turbo": "^2.10.12"
  }
}
